# 当前代码方案符合性审计

> 更新日期：2026-08-25  
> 状态：已按 2026-08-24 审计结果完成代码修复与回归验证  
> 验证环境：macOS arm64、Python 3.14.3、PySide6、PyInstaller 6.22.2

## 1. 修复结论

上一版审计中的代码级 P0/P1 问题已修复：

- 测试和 GUI 后台任务不再因缺失的 `started` 信号挂起；
- worker 无论准备、执行、成功或异常都会进入 `finally` 并发出 `finished`；
- 旧测试已适配“输出根目录必须是绝对路径”的安全规则；
- 正式预览改为后台规划，不再在 GUI 线程计算全部任务；
- 预览保存 `MergePlanResult`，上游输入未变时，实际执行直接复用同一计划；
- 模板、映射、空字段策略或输出规则变更时，缓存计划会立即失效；
- 预览后若目标文件被外部创建，执行会拒绝覆盖，也不会在异常清理时误删既有文件；
- 步骤 3 实时示例已改用正式规划器，与空字段策略、路径规则和文件名规则保持一致；
- 冲突摘要现在每个 job 只记录一条，`assigned` 为最终分配文件名；
- Word COM 完整模式对 `.docx/.docm/.doc` 统一使用 COM 扫描，扫描与替换共用 story-range 遍历器；
- 深色模式的底部操作区、内嵌输出规则输入框、语义状态文字和变量图标已改为可随主题刷新；
- 关闭窗口不再在 GUI 线程调用 `waitForDone(3000)`，改为定时器非阻塞轮询；
- 文件写入任务未完成时不允许强制退出，避免文档损坏。

## 2. 验证结果

| 检查 | 结果 |
|---|---|
| 全部自动化测试，外层 60 秒硬超时 | **112 项全部通过，约 2.9 秒结束** |
| worker 实际 `run()` 生命周期回归 | 通过 |
| GUI 异步扫描回归 | 通过，无挂起 |
| 后台预览、计划缓存与失效 | 通过 |
| 预览后外部同名文件保护 | 通过，既有文件未被覆盖或删除 |
| Word COM 完整模式调用路由测试 | 通过 |
| 主题 QSS、变量面板、文件胶囊 | 通过 |
| `compileall` | 通过 |
| `git diff --check` | 通过 |
| 未使用导入扫描 | 生产模块仅剩明确的兼容重导出和平台条件导入 |
| macOS PyInstaller 构建 | **成功** |
| macOS 打包产物离屏启动冒烟 | **成功，进程稳定运行** |

macOS 构建产物：

- `dist/WordTextReplacer.app`
- `dist/WordTextReplacer`

PyInstaller 警告仅包含 Windows 专用模块、条件导入和可选依赖，未发现当前 macOS 运行路径的必需模块缺失。

## 3. 方案符合性

| 要求 | 结论 |
|---|---|
| PySide6 跨平台界面 | 符合 |
| 四步直觉化流程 | 符合 |
| 多模板选择与逐模板输出规则 | 符合 |
| 输出目录与文件名支持变量 | 符合 |
| 绝对根目录与路径越界保护 | 符合 |
| 空字段“保持原变量/替换为空/自定义” | 实时示例、正式预览、路径、文件名和 Word 内容已统一 |
| 内嵌变量面板、紧凑标签和流式换行 | 符合 |
| 删除系统变量说明文字 | 可见文字与内部 `desc` 数据均已删除 |
| 预览与执行共享不可变任务计划 | 符合；输入变更时显式失效 |
| 大规模预览不阻塞 GUI | 符合；已改为 worker |
| 深色模式无浅色底栏/固定白边 | 代码与自动化层面符合；仍建议 Windows 11 真机截图验收 |
| Word COM 扫描与替换范围一致 | 代码路由已统一；仍需 Windows + Word 真机验收 |

## 4. 已删除的高置信冗余

- 残留的 `started.emit()`；
- `TaskState.IDLE`、`TaskState.RUNNING`；
- 一次性 token 不需要的 `CancellationToken.reset()`；
- 未读取的 `generate_preview_snippets(fields=...)` 参数；
- 无有效调用的 `build_output_destination()`；
- 未使用局部变量 `reason`；
- 无引用 GUI 兼容别名 `scan_template()`；
- 只被旧测试使用的 `TemplateSourceModel`；
- 当前界面无对应控件的 `apply_base_directory()`；
- 无按钮或信号入口的 `_clear_templates()`；
- 系统变量的冗余 `desc` 字段与搜索分支；
- 多批未使用的 Python/Qt 导入。

为兼容现有测试、PyInstaller 和可能的外部 Python 脚本，以下项目仍保留，未进行冒险删除：

- 顶层 `replacer_core.py`、`template_merge.py`、`platform_capabilities.py` 兼容重导出；
- 单模板兼容包装；
- `plan_merge_jobs()` 的 list 返回兼容 API；
- `mapped_row_values()` 和 `build_output_filename()` 旧 API；
- UI 模块中的无 Qt fallback。

## 5. 仍需外部环境完成的项目

### 5.1 Git 快照

当前工作区仍包含大量用户此前的未提交和未跟踪变更。本次没有自动提交、删除或改写与审计无关的用户改动。

在发布前必须：

1. 确认新模块、新测试和方案文档的 Git 跟踪范围；
2. 提交完整快照；
3. 在全新 clone 目录重跑测试和打包。

### 5.2 Windows 真机

当前 macOS 环境无法替代以下验收：

- Windows 11 深浅色界面截图对比；
- Windows + Microsoft Word COM 的正文、页眉页脚、文本框、脚注与尾注处理；
- Windows PyInstaller 便携版与安装版构建。

## 6. 最终评价

当前代码已从“后台任务全面失效、测试会挂起”恢复到“自动化测试全部通过、macOS 可成功打包并启动”的状态。审计中可在当前环境安全完成的代码修复和高置信冗余清理均已完成。

正式发布前仍需收口 Git 快照，并在 Windows 11 + Microsoft Word 环境完成真机验收。
