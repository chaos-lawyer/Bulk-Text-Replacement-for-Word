# 「模板批量生成」功能强化方案（直觉化流程修订版）

> 更新日期：2026-08-24  
> 适用对象：当前 PySide6 多模板批量生成界面  
> 本文已完整替代上一版《模板批量生成功能强化方案》

## 1. 修订目标

当前界面将输出目录和文件名规则放在步骤 1，但用户通常需要先知道 Excel 列、Word 模板变量和字段映射结果，才能正确填写这些规则。因此用户必须先查看步骤 2，再返回步骤 1，操作顺序与用户思考顺序相反。

推荐将流程调整为：

```text
步骤 1：选择模板与数据
  → 步骤 2：确认字段映射
    → 步骤 3：设置输出目录与逐模板文件名
      → 步骤 4：预览并生成
```

核心原则：

- 在变量尚未读取时，不要求用户填写变量规则；
- 输出目录和文件名配置全部放在字段扫描之后；
- 每一步只要求用户完成当前能够理解和验证的任务；
- 修改上游数据后，下游结果自动失效并重新计算；
- 预览和实际生成使用同一份不可变任务规划结果。

## 2. 当前代码与界面问题

当前代码已经具备：

- 多 Word 模板添加、扫描和批量生成；
- Excel/CSV、多工作表读取；
- 多模板字段并集与自动映射；
- 每字段空值处理；
- `TemplateMergeItem.filename_rule` 模板级命名字段；
- 输出子目录变量；
- `模板名`、`数据序号`、`Excel行号`、`日期`等系统变量；
- 任务预览、进度、取消和结果日志；
- 输出路径安全清理和同名避让。

主要体验问题：

1. 输出设置早于变量扫描，用户不知道可用变量；
2. 步骤 1 同时承担数据源、模板、输出目录和命名规则，信息过载；
3. 扫描后变量只出现在字段映射表，没有直接服务于输出规则编辑；
4. 用户需要手工记忆并输入 `{{变量名}}`；
5. 文件名和目录的实际效果只能在预览日志中看到，反馈距离过远；
6. 映射、模板名称或输出规则变化后，缺少清晰的依赖失效提示；
7. 同批任务重名与磁盘已有文件重名都被自动编号处理，用户不易判断规则是否合理；
8. 当前三步流程把“设置选项”和“执行日志”混在一起，完成状态不清晰。

## 3. 推荐的四步工作流

### 3.1 步骤 1：选择模板与数据

### 用户目标

只回答两个问题：

1. 使用哪些 Word 模板？
2. 使用哪份 Excel/CSV 数据？

### 界面内容

```text
步骤 1：选择模板与数据

Word 模板                         [添加模板…] [移除所选] [清空]
┌──────────────────────────────────────────────────────────┐
│ 采购合同.docx                                            │
│ 保密协议.docx                                            │
│ 授权委托书.docx                                          │
└──────────────────────────────────────────────────────────┘

数据表格：[........................................] [浏览…]
工作表：  [........................................]

                                      [读取模板与数据]
```

本步骤不显示：

- 输出目录；
- 文件名规则；
- 变量插入菜单；
- Word COM 生成选项；
- 生成日志。

### 模板来源列表

步骤 1 的列表只负责文件来源管理，不是最终的输出命名表。建议显示模板文件名，并支持：

- 一次选择多个 `.docx`、`.docm`；
- Windows 完整模式下支持 `.doc`；
- 按规范化绝对路径去重；
- 悬停显示完整路径；
- 键盘 Delete 移除；
- 文件不存在或格式不支持时就地提示。

避免在此处展示检测变量、扫描状态和文件名规则，以免用户在尚未扫描时处理下游信息。

### 扫描触发方式

推荐采用“自动扫描为主、手动重试为辅”：

- 模板和数据文件均有效后，短暂防抖后自动读取；
- 数据文件有多个工作表时，先等待用户选择工作表，再扫描；
- 保留“读取模板与数据”按钮，用于明确触发或失败后重试；
- 扫描在后台线程执行，期间不阻塞窗口；
- 每次扫描使用 generation ID，旧任务结果不得覆盖新选择。

如果担心自动读取大文件带来意外等待，可首版保留手动按钮，但按钮文案应从“扫描并匹配”改为“读取模板与数据”，更符合该步骤职责。

### 扫描结果

成功后显示摘要：

```text
已读取 3 个模板、28 个变量、500 行数据
```

存在部分失败时：

```text
2 个模板读取成功，1 个模板失败
[查看详情] [移除失败模板] [重试]
```

单个模板失败不应丢弃其他模板的成功结果。

### 3.2 步骤 2：确认字段映射与空值策略

### 用户目标

确认 Word 变量从哪一列读取，以及数据为空时如何处理。

### 映射表

建议保留当前五列结构：

| 模板变量 | 所属模板 | 对应 Excel 列 | 空字段处理 | 匹配状态 |
|---|---|---|---|---|
| `{{甲方名称}}` | 3 个模板 | 甲方名称 | 保持原变量 | 已匹配 |
| `{{合同金额}}` | 采购合同 | 合同金额 | 替换为空 | 已匹配 |
| `{{保密期限}}` | 保密协议 | 保密期限 | 自定义：长期 | 已匹配 |

规则：

- 同名变量无论出现于几个模板，只配置一次；
- “所属模板”悬停展示完整模板清单；
- 对应 Excel 列继续使用稳定的单击下拉编辑；
- 空字段处理保留“保持原变量、替换为空、自定义”；
- 映射表修改后立即刷新后续输出示例；
- 未匹配变量按空字段策略判断是否阻止继续。

### 可用变量汇总

步骤 2 完成扫描后，同时构建统一变量目录：

```text
Excel/CSV 列
Word 映射变量
系统变量
```

系统变量至少包括：

| 变量 | 含义 |
|---|---|
| `{{模板名}}` | 可编辑模板名称 |
| `{{模板文件名}}` | 源模板文件名，不含扩展名 |
| `{{模板序号}}` | 启用模板顺序 |
| `{{数据序号}}` | 有效数据顺序 |
| `{{Excel行号}}` | 原始数据行号 |
| `{{日期}}` | 当前任务规划日期 |

变量目录由步骤 3 的规则编辑器直接使用，用户不需要来回查看和记忆。

### 完成条件

满足以下条件后启用步骤 3：

- 至少一个模板扫描成功；
- 数据表至少包含一行有效数据；
- 所有必须替换的变量已映射；
- 未映射变量均有明确的空字段策略。

如仍有“保持原变量”的未映射字段，可以允许继续，但必须显示警告，并说明这些占位符会保留在 Word 中。

### 3.3 步骤 3：设置输出目录与逐模板文件名

### 用户目标

在变量已经可见、映射已经确认的前提下，决定文件保存到哪里、每个模板叫什么名字。

### 推荐布局

```text
步骤 3：设置输出目录与文件名

输出目录规则：[D:/合同输出/{{地区}}/{{客户名称}}] [浏览…] [插入变量 ▾]
示例目录：  D:/合同输出/华东/示例公司

Word 模板输出规则
┌──────┬──────────────────┬────────────────────────────────────┐
│ 启用 │ 模板名称         │ 文件名规则                         │
├──────┼──────────────────┼────────────────────────────────────┤
│  ✓   │ 采购合同         │ 采购合同-{{合同编号}}              │
│  ✓   │ 保密协议         │ {{客户名称}}-保密协议              │
│  ✓   │ 授权委托书       │ {{模板名}}-{{数据序号}}            │
└──────┴──────────────────┴────────────────────────────────────┘

当前示例：
采购合同 → D:/合同输出/华东/示例公司/采购合同-HT-001.docx
```

### 输出目录规则

“基础输出目录”和“子目录规则”合并为一个设置：

```text
D:/合同输出/{{地区}}/{{客户名称}}
```

界面只有一个输入框，但内部仍解析为：

```python
base_folder = "D:/合同输出"
relative_rule = "{{地区}}/{{客户名称}}"
```

安全规则：

- 固定根目录必须是合法绝对路径；
- 变量只能出现在固定根目录之后；
- 变量不能生成盘符、UNC 根或上级目录；
- 变量值中的 `/`、`\` 作为非法字符清理，不能创建额外层级；
- 拒绝 `.`、`..` 和路径越界；
- 最终路径必须位于固定根目录内；
- 对 Windows 保留名称和超长路径提前预警。

点击“浏览…”时：

- 没有变量后缀：用所选目录替换输入框；
- 已有变量后缀：默认只替换固定根目录并保留后缀；
- 用户可明确选择“重置为所选目录”。

### Word 模板输出规则表

步骤 3 的 Word 模板表只显示：

| 列 | 说明 |
|---|---|
| 启用 | 是否参与本次生成 |
| 模板名称 | 默认使用源文件名，可编辑 |
| 文件名规则 | 每个模板独立配置 |

不显示：

- 文件路径；
- 检测变量；
- 状态。

被隐藏的信息仍保留在模型中：

- 模板名称悬停显示完整路径；
- 错误通过名称前的状态图标和 tooltip 展示；
- 检测变量统一在步骤 2 查看；
- 模板扫描摘要显示成功与失败数量。

### 默认文件名规则

扫描成功后，为每个新模板自动设置：

```text
{{模板名}}-{{数据序号}}
```

不再提供全局文件名规则，也没有“继承全局”的概念。每个启用模板都直接持有自己的规则。

采用 `{{数据序号}}` 而不是 `{{序号}}`，因为 `{{序号}}` 可能是 Excel 列，语义会随表头变化。

文件扩展名由源模板决定，用户不必在规则中输入：

- `.docx` 模板输出 `.docx`；
- `.docm` 模板输出 `.docm`；
- Windows Word COM 下 `.doc` 模板输出 `.doc`。

### 变量插入菜单

输出目录和文件名规则均提供“插入变量”菜单：

```text
Excel/CSV 列
  客户名称
  合同编号
  地区

Word 映射变量
  甲方名称
  合同金额

系统变量
  模板名
  模板文件名
  数据序号
  Excel行号
  日期
```

点击后在光标位置插入完整占位符，例如 `{{合同编号}}`。

模板文件名规则编辑器只展示当前模板可用的 Word 变量，但 Excel 列和系统变量始终可用。

### 即时示例与校验

规则每次变化后，使用第一行有效数据即时显示：

- 解析后的目录；
- 解析后的文件名；
- 完整目标路径；
- 使用了哪些变量；
- 未知或空值变量；
- 是否与其他任务冲突。

就地状态建议：

- 正常：绿色勾选或中性“规则有效”；
- 警告：空值、磁盘已有文件、路径较长；
- 错误：未知变量、空规则、路径越界、清理后空名称。

用户不必进入步骤 4 才发现规则错误。

### 3.4 步骤 4：预览并生成

### 用户目标

确认最终任务清单，然后执行生成。

### 预览摘要

```text
启用模板：3 个
数据行：500 行
预计生成：1500 个文件
预计目录：42 个
规则错误：0
命名冲突：0
磁盘已有文件：3 个
```

### 预览结构

建议按数据行或模板分组，并允许切换：

```text
Excel 第 2 行：示例公司
├─ 采购合同
│  └─ 华东/示例公司/采购合同-HT-001.docx
├─ 保密协议
│  └─ 华东/示例公司/示例公司-保密协议.docx
└─ 授权委托书
   └─ 华东/示例公司/授权委托书-001.docx
```

首屏只展示前若干行，但规划与冲突检查必须覆盖全部任务。

### 生成操作

- “开始批量生成”是本页面唯一主按钮；
- 无错误、无警告时直接开始；
- 有警告时显示一次汇总确认；
- 有错误时禁止生成并定位到对应步骤和控件；
- 任务期间锁定所有会改变任务语义的输入；
- 支持取消，并保留已完成结果；
- 结果日志显示模板、Excel 行和最终相对路径。

## 4. 步骤间状态与导航

### 4.1 渐进启用

| 状态 | 可用步骤 |
|---|---|
| 未选择来源 | 仅步骤 1 |
| 正在扫描 | 步骤 1 显示进度，其余只读 |
| 扫描成功 | 启用步骤 2 |
| 映射可用 | 启用步骤 3 |
| 输出规则有效 | 启用步骤 4 和生成按钮 |

### 4.2 自动前进

- 扫描成功后滚动或聚焦步骤 2；
- 映射全部有效后，不强制自动跳转，但突出“下一步：设置输出”；
- 输出规则全部有效后突出“预览生成结果”；
- 不自动弹出阻断性对话框打断连续配置。

### 4.3 上游变化时的失效规则

```text
模板或数据变化
  → 重新扫描
  → 字段映射需重新确认
  → 输出示例与任务预览失效

字段映射或空值策略变化
  → 输出示例自动刷新
  → 任务预览失效

模板名称、启用状态、文件名规则或输出目录变化
  → 输出示例自动刷新
  → 任务预览失效
```

失效不等于清空：

- 按变量名保留仍然有效的映射；
- 按模板 ID 保留模板名称和文件名规则；
- 保留用户填写的输出目录规则；
- 只清除无法再验证的预览和冲突结果。

### 4.4 步骤状态表达

每个步骤标题显示状态：

```text
✓ 步骤 1：已读取 3 个模板、500 行数据
✓ 步骤 2：28/28 个变量已确认
! 步骤 3：1 个文件名规则需要处理
  步骤 4：等待输出规则完成
```

避免只通过按钮禁用表达状态，应同时显示用户下一步需要做什么。

## 5. 多模板文件命名规则

### 5.1 每模板独立

每个 `TemplateMergeItem` 直接保存：

```python
filename_rule: str
```

多模板规划只读取每个模板自己的规则，不再接受多模板全局命名规则。

单模板旧 API 可以继续把传入的 `filename_rule` 写入临时 `TemplateMergeItem`，保持兼容。

### 5.2 未知变量与空值

当前未知变量可能被静默替换为空，应改为严格规划校验：

- 变量不存在：错误，阻止生成；
- 变量存在但本行为空：按空值策略处理并显示具体行；
- “保持原变量”只适用于 Word 正文，不能让路径残留 `{{变量}}`；
- “替换为空”允许结果为空，但必须再次检查文件名和目录是否合法；
- “自定义”使用自定义值；
- 清理非法字符后文件名为空：错误。

### 5.3 冲突分类

必须区分：

1. **同批任务内部冲突**：规则本身不足以区分模板或数据；
2. **磁盘已有文件冲突**：规划本身唯一，但目标已存在。

同批内部冲突：

- 生成前列出冲突任务；
- 默认阻止生成；
- 建议修改对应模板规则或在输出目录中加入 `{{模板名}}`；
- 用户可显式确认使用 `(2)`、`(3)`，但不能静默处理。

磁盘已有文件：

- 默认继续安全编号；
- 预览显示最终编号后的文件名；
- 标明“目标已存在，已自动编号”；
- 不默认覆盖用户文件。

冲突判断基于完整目标路径，不只比较文件名。

## 6. 数据模型调整

### 6.1 模板模型

现有数据结构可继续使用：

```python
@dataclass
class TemplateMergeItem:
    template_id: str
    file_path: str
    display_name: str
    fields: list[str]
    enabled: bool
    filename_rule: str
    status: str
    error: str
```

建议：

- 添加模板时默认 `filename_rule = "{{模板名}}-{{数据序号}}"`；
- 模板 ID 使用 UUID 或规范化路径哈希，避免删除后添加造成重复；
- 扫描结果回写只更新 `fields/status/error`，不覆盖用户名称和命名规则。

### 6.2 两种模板视图

步骤 1 和步骤 3 使用同一个 `TemplateListModel` 数据源，但采用不同视图职责：

- 步骤 1：来源管理视图，只显示模板文件名，负责添加和删除；
- 步骤 3：输出规则视图，显示“启用、模板名称、文件名规则”。

两处不复制业务数据，所有修改落在同一组 `TemplateMergeItem` 上。

也可以在步骤切换时移动同一个 `QTableView`，但实现复杂且容易造成布局和焦点问题，不建议。使用两个轻量视图共享同一模型更清晰。

### 6.3 输出目录规则

建议增加：

```python
@dataclass
class OutputDirectoryRule:
    raw_rule: str
    base_folder: str
    relative_rule: str
```

解析函数：

```python
def parse_output_directory_rule(rule: str) -> OutputDirectoryRule: ...
def render_output_directory(rule, values) -> Path: ...
```

界面是一个设置，Core 内部保留固定根目录与相对变量目录的安全边界。

### 6.4 规划结果

建议增加：

```python
@dataclass
class MergePlanIssue:
    severity: str
    code: str
    message: str
    template_id: str = ""
    excel_row: int = 0
    rule: str = ""


@dataclass
class MergePlanResult:
    jobs: list[MergeJob]
    issues: list[MergePlanIssue]
    batch_conflicts: list
    existing_file_conflicts: list
```

预览与执行共用同一个 `MergePlanResult` 或其不可变任务快照。

## 7. Core 与 Service 调整

### 7.1 Core 推荐入口

```python
def plan_merge_jobs(
    templates,
    data,
    mapping,
    output_directory_rule,
    default_values=None,
    empty_field_behaviors=None,
) -> MergePlanResult:
    ...
```

多模板入口不再传入：

```python
default_filename_rule
base_output_folder
relative_folder_rule
```

执行阶段接收已经确认的任务清单，避免重新规划导致预览与实际输出不同。

### 7.2 两阶段路径规划

当前路径函数会在规划时直接追加 `(2)`。建议调整为：

```text
第一阶段：解析所有原始目录与文件名
第二阶段：检测未知变量、非法路径和同批内部冲突
第三阶段：根据用户确认处理内部冲突
第四阶段：检测磁盘已有文件并安全编号
第五阶段：冻结最终 MergeJob 清单
```

### 7.3 Service 职责

`MergeService` 负责：

- 异步读取所有模板和数据；
- 返回模板级成功、警告与失败；
- 构建字段并集和变量目录；
- 调用统一规划器生成即时示例和完整预览；
- 阻止含错误的任务进入写阶段；
- 聚合成功、部分成功、全部失败和取消状态；
- 让日志始终包含模板名称、Excel 行和最终路径。

## 8. 其他建议改进

### 8.1 规则模板和快捷插入

提供常用模板：

```text
{{模板名}}-{{数据序号}}
{{模板名}}-{{合同编号}}
{{客户名称}}-{{模板名}}
{{日期}}-{{模板名}}-{{数据序号}}
```

用户选择后只影响当前模板，不批量覆盖其他模板。

可另提供“应用到所选模板”，但必须显示将被修改的模板数量并允许撤销。

### 8.2 规则有效性而非单纯必填

不要只判断输入框非空。必须实际用数据规划：

- 规则引用的变量是否存在；
- 每行解析后是否为空；
- 是否产生同批冲突；
- 是否生成非法或超长路径；
- 是否覆盖源模板；
- 是否与磁盘已有文件冲突。

### 8.3 预览定位

预览发现问题时提供“定位”操作：

- 文件名问题 → 定位步骤 3 对应模板行；
- 字段空值问题 → 定位步骤 2 对应字段；
- 数据来源问题 → 定位步骤 1；
- 输出目录问题 → 聚焦步骤 3 输出目录规则。

### 8.4 大任务性能

- 即时示例只计算第一行或当前选中行；
- 完整规划在后台执行；
- 预览列表使用分页或虚拟化；
- 冲突检测使用规范化路径哈希集合；
- 生成进度总数为启用模板数 × 有效数据行数；
- 取消在每个“模板 × 数据行”任务边界检查。

### 8.5 无障碍与主题

- 每个步骤卡片有明确 accessible name 和完成状态；
- 规则错误不能只依赖颜色，应同时显示图标和文字；
- 文件名规则支持键盘编辑与变量菜单；
- 编辑器关闭后主动刷新 viewport，避免文字重影；
- 深色模式下表格 viewport、编辑器、tooltip、菜单和边框全部使用主题 token；
- 125%/150% 缩放下不截断关键操作。

## 9. 测试要求

### 9.1 流程测试

- 未扫描时不要求填写输出规则；
- 扫描成功后步骤 2 自动可用；
- 映射满足条件后步骤 3 可用；
- 输出规则有效后步骤 4 和生成按钮可用；
- 用户不需要返回上一步查找变量；
- 上游变化正确失效下游预览，但尽量保留有效设置。

### 9.2 模板与映射测试

- 多模板添加、去重、移除和部分扫描失败；
- 同名 Word 变量只显示一次；
- 所属模板关系正确；
- 映射和空字段三策略正确；
- 扫描结果不覆盖模板名称和文件名规则。

### 9.3 输出规则测试

- 步骤 3 模板表只显示启用、模板名称、文件名规则；
- 每个模板可保存不同规则；
- 新模板默认规则为 `{{模板名}}-{{数据序号}}`；
- 编辑一个模板不影响其他模板；
- 变量菜单插入正确占位符；
- 模板名称修改后即时示例更新；
- 未知变量、空规则和空值准确提示。

### 9.4 输出目录测试

- 固定绝对路径正确解析；
- 固定根目录加多层变量正确解析；
- 浏览替换根目录时保留变量后缀；
- Windows 盘符、UNC 和 macOS 路径正确；
- 变量不能出现在安全根目录之前；
- `/`、`\`、`..` 和非法字符不能造成路径越界；
- 空值策略和超长路径提示正确。

### 9.5 规划与生成测试

- 预览和实际生成路径完全一致；
- 同批内部冲突在自动编号前被检测；
- 不同目录的相同文件名不判冲突；
- 磁盘已有文件安全编号且不覆盖；
- 扩展名跟随 `.docx`、`.docm`、`.doc` 模板；
- 单模板旧 API 兼容；
- 取消和部分失败结果准确。

### 9.6 GUI 测试

- 四步卡片状态、焦点和自动前进正确；
- 两个模板视图共享同一数据模型且不互相覆盖；
- 下拉框、文件名编辑器和变量菜单点击稳定；
- 深色模式无白色边框、文字重影或编辑器残影；
- 高 DPI 和窗口缩放下关键内容可见。

## 10. 实施顺序

### P0：流程与规划基础

1. 将页面重排为四步；
2. 将输出配置移到扫描和映射之后；
3. 新增统一变量目录；
4. 每模板写入独立默认文件名规则；
5. 合并输出目录设置并实现安全解析；
6. 拆分同批冲突与磁盘冲突；
7. 让预览和执行共享任务规划结果。

### P1：交互完善

1. 增加步骤状态与下游失效提示；
2. 增加变量插入菜单；
3. 增加即时目录、文件名和完整路径示例；
4. 增加问题定位与模板级错误提示；
5. 完成模板规则编辑器和深浅色适配。

### P2：效率增强

1. 自动扫描与防抖；
2. 常用规则模板；
3. 批量应用规则并支持撤销；
4. 大任务后台规划和虚拟化预览；
5. 输出规则历史或用户主动保存的任务预设。

## 11. 验收标准

1. 用户按页面顺序操作时不需要返回上一步查找变量；
2. 步骤 1 只要求选择模板和数据；
3. 步骤 2 清楚展示全部可映射变量和空值策略；
4. 步骤 3 才出现输出目录与文件名设置，并可直接插入已读取变量；
5. Word 模板输出表只显示启用、模板名称、文件名规则；
6. 每个模板具有独立文件名规则，不存在全局文件名规则；
7. 输出目录使用一个可包含变量的设置；
8. 每次修改规则后立即显示真实路径示例和校验结果；
9. 未知变量、空值、非法路径和同批冲突在生成前被发现；
10. 预览和实际生成使用完全一致的任务路径；
11. 上游数据变化后下游状态正确失效，仍有效的设置尽量保留；
12. 单模板、Excel/CSV、多工作表、空字段三策略继续兼容；
13. Windows、macOS 快速模式和 Windows Word COM 模式完成实机验收；
14. 深色模式下新增步骤、编辑器和变量菜单无白边、重影或文字重叠。

## 12. 推荐最终体验

用户首先只选择模板和数据。程序读取完成后，用户在步骤 2 确认字段映射；此时系统已经知道全部 Excel 列、Word 变量和系统变量。

进入步骤 3 后，用户不需要记忆变量名称，可以直接通过菜单设置：

```text
输出目录规则：D:/合同输出/{{地区}}/{{客户名称}}

采购合同：采购合同-{{合同编号}}
保密协议：{{客户名称}}-保密协议
授权委托书：{{模板名}}-{{数据序号}}
```

界面立即展示第一行数据的真实目录和文件名。所有规则有效后，步骤 4 汇总完整任务清单、冲突和磁盘已有文件，再执行生成。

这一流程与用户的自然思考顺序一致：先决定“用什么数据”，再确认“变量怎么对应”，然后设置“输出到哪里、叫什么”，最后确认并生成。
