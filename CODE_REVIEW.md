# 代码审计报告（更新后复审）

> 审计日期：2026-08-24
>
> 审计范围：当前工作区全部源代码、构建脚本、测试、文档及更新后的多模板功能
>
> 审计原则：保持现有功能和用户行为；优先修复明确缺陷与可靠性问题；不建议无收益的大规模重写

## 1. 执行摘要

本次复审基于更新后的代码重新梳理了应用入口、三条业务流程、应用服务层、核心 Word/Excel 处理、异步任务、Qt 界面、Windows COM 自动化、文件保存与构建配置，而非沿用旧报告结论。

当前项目的整体方向是合理的：PySide6 界面、`application` 编排层、`core` 业务层和 `platform_adapter` 平台适配层已形成基本边界；旧 Tkinter 版本已移入 `_legacy`；快速模式的多命中替换、零命中不写盘、安全备份和校验后原子替换均有实质改善。现有代码不需要整体重写。

但当前版本仍不适合直接发布，主要原因如下：

- **P0：新版多模板批量生成可以预览，却无法正式执行。** 界面只配置每个模板的完整输出规则，但执行服务仍强制要求已经取消的全局输出目录，因此会直接返回“请指定输出目录规则”。
- **P1：用户已确认的胶囊悬浮窗口只能出现一次。** 事件过滤器在 `MouseButtonPress` 阶段重新显示一个 `Qt.Popup`，与 Qt 的点击外部自动关闭时序冲突；输入框已经获得焦点后，后续点击很可能把刚显示的窗口立即关闭。
- 全局任务协调器宣称支持多个只读任务，内部却只能保存一个任务；扫描和预览又没有注册，快捷键可以绕过按钮禁用，跨页面读写任务仍可能重叠。
- `.doc` 文件在界面层使用默认“快速模式”提前校验，因此用户即使选择完整模式也会被拒绝。
- 完整模式的扫描范围和执行范围不一致，COM 保存也没有快速模式现有的原子写入保护。
- 更新后的不可见字符处理、模糊字段映射和多行替换存在文档内容或格式回归风险。

优先建议先完成第 5.1～5.4 节中的阻断性和高可靠性修复，再进行 Windows + Microsoft Word 真机回归。其余结构优化可以渐进实施。

## 2. 项目结构与主要流程

### 2.1 当前架构

| 层次 | 主要目录/文件 | 现状评价 |
|---|---|---|
| 启动与窗口 | `src/app.py`、`src/ui/main_window.py` | 入口清楚；关闭任务的策略仍需修正 |
| Qt 页面 | `src/ui/pages/replace_page.py`、`merge_page.py`、`multi_doc_page.py` | 三条业务流程集中在页面类中；部分页面过大，但暂不值得为拆分而拆分 |
| 应用编排 | `src/application/*_service.py`、`workers.py`、`task_coordinator.py` | 服务层方向正确；任务协调和参数契约存在缺口 |
| 核心业务 | `src/core/replacer_core.py`、`template_merge.py`、`multi_doc_replacer.py` | 可脱离 GUI 测试；多模板规划与执行契约尚未统一 |
| 平台适配 | `src/platform_adapter/*`、`src/core/word_com.py` | Windows COM 生命周期已有集中封装；能力探测和保存安全仍不足 |
| 数据模型 | `src/core/models.py`、`src/ui/models/*` | 模型化程度提升；后台任务仍收到 GUI 持有的可变对象 |
| 构建与发布 | `*.spec`、`build_*.bat`、`build_macos.sh` | 多平台脚本齐全；缺少 Windows 构建/Word 真机自动验证 |
| 历史代码 | `_legacy/v2_tkinter` | 已与当前实现隔离，处理合理 |

### 2.2 核心业务流程

1. **文本查找替换**：页面收集文件和选项 → `ReplaceService` 校验并编排 → 快速模式使用 `python-docx`，完整模式使用 Word COM → 返回逐文件结果。
2. **模板批量生成**：读取多个模板和 Excel/CSV → 扫描变量并建立映射 → 为模板 × 数据行规划输出目标 → 复制模板并替换变量。
3. **多文档匹配替换**：扫描每份文档变量 → 用户为各文档填写差异数据 → 原位备份或复制到输出目录 → 快速/COM 替换。

整体职责划分已经可维护。当前最重要的不是增加抽象层，而是让“界面显示的计划、预览计划、执行计划”成为同一个确定性契约。

## 3. 验证结果

### 3.1 已执行检查

- `QT_QPA_PLATFORM=offscreen .venv/bin/python -m unittest discover -s tests -v`
  - **92 项测试全部通过**，耗时约 1.5 秒。
- `.venv/bin/python -m compileall -q src tests`
  - 编译通过，但 `src/core/replacer_core.py` 的文档字符串产生 `invalid escape sequence '\g'` 警告。
- 针对多模板流程构造最小调用：
  - `generate_preview_snippets` 能生成有效目标路径；
  - 相同参数调用 `execute_multi_template_merge` 返回失败，错误为“请指定输出目录规则”。
- 常规 GUI 测试命令在当前 macOS 桌面环境初始化系统粘贴板服务时退出（状态码 134）；改用 Qt offscreen 后测试可稳定运行。该现象不能替代 Windows 真机验证。

### 3.2 测试覆盖缺口

现有测试没有覆盖以下发布关键路径：

- 从 `MergePage` 收集参数后完成真实的多模板执行；
- 胶囊窗口关闭后重复点击同一输入框；
- 完整模式下 `.doc` 的页面级校验；
- 快捷键重复触发、跨页面扫描与写任务互斥；
- Windows Word COM 的正文、页眉页脚、文本框、脚注、尾注以及保存失败恢复；
- 不可见字符夹在搜索词或模板变量中的实际替换；
- 多行替换后的完整字符格式和超链接结构。

## 4. 已确认改善与无需修改项

以下更新有明确收益，应保留，不建议为了“更现代”再次重构：

- 旧 Tkinter 实现已移入 `_legacy/v2_tkinter`，当前 `src` 不再混用两个 GUI 框架。
- `ReplaceService`、`MergeService`、`MultiDocService` 将页面与核心处理隔开，方向正确。
- 快速替换使用字符到 run 的映射，已支持单段落多个匹配、跨 run 匹配和正则反向引用；相比旧实现更可靠。
- 快速模式零命中时不创建备份、不保存文件，避免无意义地改写文档。
- `create_safe_backup` 不再覆盖既有备份，会生成带序号的新备份。
- `atomic_save_docx` 在同目录写临时文件、校验 ZIP/Word 主文档结构后再 `os.replace`，是合理的低复杂度保护。
- 文件格式规则集中到 `core/format_policy.py`，避免各页面自行维护扩展名集合。
- Word COM 会话集中到 `WordAutomationSession`，包含 `DispatchEx`、`Quit` 和线程 COM 清理，基本方向正确。
- 模板输出文件扩展名现在由源模板决定，避免扩展名与内容格式不一致。
- 历史兼容包装保留在核心函数边界，没有发现必须立即删除的死代码。

## 5. 审计发现与可执行改进项

### 5.1 P0：多模板预览成功但正式生成必然被旧参数校验拦截

- **当前问题**：`MergePage` 把每个模板的“完整输出路径与文件名规则”保存在 `TemplateMergeItem.filename_rule` 中，预览能直接据此规划任务；正式执行却传入空的 `output_directory_rule`。`MergeService.execute_multi_template_merge` 在读取模板规则前强制要求全局输出目录非空，`generate_multi_template_batch` 也重复检查全局根目录。
- **涉及文件**：
  - `src/ui/pages/merge_page.py`（`preview_merge`、`start_batch_merge`）
  - `src/application/merge_service.py`（`generate_preview_snippets`、`execute_multi_template_merge`）
  - `src/core/template_merge.py`（`plan_merge_jobs`、`generate_multi_template_batch`）
- **为什么需要修改**：这是当前主功能的发布阻断缺陷；预览还会向用户展示一个实际上无法执行的计划。
- **推荐修改方式**：
  1. 让 `plan_merge_jobs` 成为输出路径合法性和冲突解析的唯一入口；每个模板规则包含目录时直接使用该目录，只有规则仅含文件名时才使用明确的共享默认根目录。
  2. 删除服务层和执行层在规划前对旧全局目录的无条件检查；规划结果为空或某模板缺少有效固定根目录时，返回精确到模板的校验错误。
  3. 预览阶段生成一个不可变 `MergePlan`（包含任务、输入快照和冲突处理结果），执行阶段消费同一计划或用同一纯函数重新生成并校验计划摘要，避免两条路径漂移。
  4. 对输出根目录按模板去重后创建，不要只创建一个全局目录。
- **修改收益**：恢复新版多模板正式生成功能；保证预览与执行一致；简化重复分支。
- **风险/影响范围**：涉及新旧单模板兼容包装。需保留 `execute_batch_merge` 的旧参数行为，并补充两类兼容测试。
- **验收测试**：至少覆盖“每模板绝对路径规则”“仅文件名 + 共享目录”“两个模板不同根目录”“目录含变量”“磁盘重名”“预览目的地等于实际目的地”。

### 5.2 P1：胶囊悬浮窗口仅首次点击可见（用户已确认）

- **当前问题**：设置输出路径时，胶囊悬浮窗口第一次点击输入框会显示；关闭、点击外部或插入变量后，再点击同一输入框无法再次显示。
- **涉及文件**：
  - `src/ui/pages/merge_page.py`（`_PopupEventFilter.eventFilter`、`_open_var_popup_for`）
  - `src/ui/widgets/variable_popup.py`（`VariableCapsulePopup`）
- **原因判断**：事件过滤器在 `MouseButtonPress` 和 `FocusIn` 两个阶段直接调用显示。第一次点击通常同时触发鼠标和获得焦点；后续输入框已经有焦点，只剩鼠标按下。此时 `Qt.Popup` 正在处理一次“点击弹窗外部”的自动关闭流程，新显示的弹窗可能在同一事件分发中被立即关闭，因此视觉上像没有出现。当前 offscreen 环境无法可靠模拟原生窗口焦点，但代码时序与用户现象一致。
- **推荐修改方式**：
  1. 将显示动作排到当前鼠标事件之后，例如用 `QTimer.singleShot(0, ...)`，或改在 `MouseButtonRelease` 处理。
  2. 只保留一个集中式 `show_for(line_edit)` 入口，明确“重复点击保持显示/重新显示”行为；避免 `MouseButtonPress` 与 `FocusIn` 对同一次交互重复调用。
  3. 弹窗隐藏时清理或更新目标引用，但不要依赖输入框再次获得焦点才显示。
  4. 如果目标控件已删除，使用 `QPointer` 或销毁信号清除引用。
- **修改收益**：恢复路径变量选择器的持续可用性，减少输入复杂规则时的阻断感。
- **风险/影响范围**：仅影响弹窗显示时序；需检查点击胶囊时不会因为目标输入框重新获得焦点而立刻再弹一次。
- **验收测试**：原生 Windows 上连续执行“打开 → 点击外部关闭 → 再打开”至少 3 次；再覆盖关闭按钮、插入胶囊、切换模板行、删除当前行后点击其他行。

### 5.3 P1：任务协调器的数据结构与并发规则不一致，扫描/预览未纳入协调

- **当前问题**：`TaskCoordinator` 允许多个只读任务并发，却只有单个 `_active_task_id` 和 `_active_worker`，第二个只读任务会覆盖第一个；旧任务完成也无法正确表示全局状态。实际上页面只注册写任务，扫描和异步预览没有注册。按钮虽然被禁用，主窗口快捷键仍可再次调用页面方法。
- **涉及文件**：`src/application/task_coordinator.py`、`src/ui/main_window.py`、三个 `src/ui/pages/*_page.py`、`src/application/workers.py`。
- **为什么需要修改**：跨页面扫描可与写任务同时访问相同文档；重复快捷键可启动多个后台任务；底部取消按钮会向三个页面的最后一个 worker 广播，任务归属不明确。
- **推荐修改方式**：优先采用简单策略：全应用同一时间只允许一个用户可见后台任务，并由协调器保存任务 ID、worker、类型和页面；所有扫描、异步预览和写任务统一注册/释放/取消。若确有并行只读需求，再改为 `dict[task_id, TaskRecord]`，不要继续使用单槽状态冒充多任务管理器。
- **修改收益**：消除重入、错误取消、读写竞态和状态栏互相覆盖。
- **风险/影响范围**：会使不同页面的只读任务串行化，但对桌面工具更可预测，性能影响很小。
- **验收测试**：重复快捷键、扫描中切页后执行写任务、旧任务晚到的 finished 信号、取消后立即启动新任务、两个只读任务的注册/释放顺序。

### 5.4 P1：完整模式 `.doc` 在页面层被默认快速模式错误拒绝

- **当前问题**：文本替换页面先调用 `ReplaceService.validate_inputs(paths, search_text)`，随后才计算 `mode`；多文档页面也未把 `use_com` 传给首次校验。因此 `.doc` 会按默认快速模式拒绝，即使用户已经选择完整模式。服务执行阶段虽然会按正确模式再次校验，但永远到不了该阶段。
- **涉及文件**：`src/ui/pages/replace_page.py`、`src/ui/pages/multi_doc_page.py`、`src/application/replace_service.py`、`src/application/multi_doc_service.py`。
- **推荐修改方式**：先冻结页面选项快照，再把 `mode`/`use_com` 传入唯一一次服务校验；不要在 UI 和执行服务中使用不同默认值。可让服务返回结构化验证结果供页面显示。
- **修改收益**：恢复 Windows 完整模式对 `.doc` 的承诺，减少重复校验漂移。
- **风险/影响范围**：低；快速模式仍应明确拒绝 `.doc`。

### 5.5 P1：后台任务直接使用 GUI 持有的可变模型对象

- **当前问题**：`TemplateListModel.get_enabled_templates()` 和 `get_templates()` 只复制列表，元素仍是原 `TemplateMergeItem`。扫描 worker 会修改其 `fields/status/error`；生成 worker 也直接读取同一对象。任务期间输出规则表、映射表、Excel 路径和完整模式选项并未全部锁定，用户编辑可能改变正在执行的任务。
- **涉及文件**：`src/ui/models/template_list_model.py`、`src/ui/pages/merge_page.py`、`src/core/models.py`。
- **推荐修改方式**：启动任务前使用 `dataclasses.replace`/深复制构造不可变输入快照；worker 只返回结果，不直接修改 GUI 模型对象。任务期间禁用所有会改变语义的控件，完成后按稳定的 `template_id` 合并结果。
- **修改收益**：任务结果可复现，避免跨线程读写 Python/Qt 状态。
- **风险/影响范围**：需要明确扫描结果回填策略，但不改变用户功能。

### 5.6 P1：完整模式扫描与执行的 Word 内容范围不一致

- **当前问题**：选择完整模式时，`.docx/.docm` 仍通过 `python-docx` 扫描变量，仅 `.doc` 使用 COM；执行却可能通过 COM 处理更多 StoryRanges。页眉页脚、文本框、脚注或尾注中的变量可能执行时可替换，但扫描和映射阶段完全看不到。
- **涉及文件**：`src/application/merge_service.py`、`src/core/multi_doc_replacer.py`、`src/core/template_merge.py`、`src/core/word_com.py`。
- **推荐修改方式**：完整模式下所有支持格式统一使用 COM 扫描；或者明确实现并标注混合模式的覆盖范围。扫描和执行应复用同一个 story-range 遍历器，避免各自维护循环。
- **修改收益**：用户看到的变量列表与最终替换范围一致。
- **风险/影响范围**：COM 扫描更慢；应显示进度并允许在文档边界取消。

### 5.7 P1：COM 保存缺少故障恢复，零命中也会备份和改写

- **当前问题**：快速模式已有校验后原子替换；COM 路径仍直接打开目标并 `Save()`。原位替换在确认命中前就创建备份，零命中也会保存。Word 崩溃、磁盘不足或网络盘中断时，目标文件的安全性依赖 Word 自身行为。
- **涉及文件**：`src/core/replacer_core.py`、`src/core/template_merge.py`、`src/core/multi_doc_replacer.py`、`src/core/word_com.py`。
- **推荐修改方式**：先在只读/重复 Range 中统计命中；有命中后再备份。对可行格式使用同目录临时副本执行 COM 修改并关闭，验证可重新打开后再替换目标；至少保证失败时保留原文件和临时恢复路径。对生成到新文件的流程，失败后安全删除不完整输出即可。
- **修改收益**：降低文档损坏和无意义改写风险，统一快慢模式语义。
- **风险/影响范围**：Word 的 `SaveAs2`、宏文档格式码和受保护视图需要 Windows 真机验证，不能只靠 mock。

### 5.8 P1：不可见字符“可匹配”能力发生回归，扫描与替换可能不一致

- **当前问题**：README 仍宣称自动清理软连字符、零宽字符并规范 NBSP。当前快速替换的正则在原始文本上匹配，`strip_invisible_chars` 只用于上下文展示；测试只验证清理辅助函数和“零命中不修改”，没有验证 `甲方` 能匹配 `甲\u00ad方`。模板字段扫描可能把带不可见字符的变量规范成正常字段，但实际替换仍在原文上搜索正常占位符，导致扫描到却替换不到。
- **涉及文件**：`README.md`、`src/core/replacer_core.py`、`src/core/template_merge.py`、相关测试。
- **推荐修改方式**：建立“规范化文本索引 → 原始字符位置”的映射，在规范化视图上匹配、在原始 run 位置上修改；不要先破坏性清洗全部 run。NBSP 是否等价于普通空格应作为显式规则并同时用于预览、计数和执行。
- **修改收益**：恢复文档从网页、PDF 或扫描系统复制文本时的实际可替换性，同时保持零命中不改文档。
- **风险/影响范围**：位置映射需覆盖跨 run 和正则；必须以测试驱动实现，避免格式回归。

### 5.9 P1：模糊自动映射可能静默选择错误的业务字段

- **当前问题**：`build_field_mapping` 在精确匹配失败后使用双向子串匹配，并采用第一个命中的 Excel 列。例如“名称”面对“甲方名称”和“乙方名称”时会静默选择第一列。对于合同类批量文档，错误替换比未匹配更危险。
- **涉及文件**：`src/core/template_merge.py`、`src/ui/models/field_mapping_model.py`。
- **推荐修改方式**：仅自动接受规范化后的唯一精确匹配；模糊候选为 1 个时也建议标为“建议匹配”并要求确认，候选多个时保持未匹配并展示候选列表。不要引入复杂相似度库。
- **修改收益**：避免批量生成大量内容错误但表面成功的文档。
- **风险/影响范围**：部分现有数据需要用户多点一次确认，但这是合理的安全交换。

### 5.10 P1：多行替换只复制少量字符格式，复杂 Word 内容可能失真

- **当前问题**：模板替换把包含换行的值拆成多个段落时，只复制粗体、斜体、下划线、字体、字号和颜色；高亮、删除线、上下标、语言、字符样式等 `rPr` 属性可能丢失。超链接内部的 run 也需要单独验证，多行值可能破坏超链接包装或段落结构。
- **涉及文件**：`src/core/template_merge.py`（段落 run 收集、格式复制、换行拆分）。
- **推荐修改方式**：优先深复制原 run 的 `w:rPr` 和段落属性，而不是逐字段枚举格式；对超链接 run 保持其 XML 容器关系。为无法合理保真的情形制定明确行为，而非静默降级。
- **修改收益**：降低合同模板排版损坏风险。
- **风险/影响范围**：直接操作 OOXML 需小范围实现并增加解压 XML 断言，不建议重写整个替换引擎。

### 5.11 P2：输出规则的实时示例、预览和执行仍有三套回退逻辑

- **当前问题**：实时示例在规则仅含文件名时回退到“模板目录/Generated”；任务规划则依赖传入的全局规则。弹窗还提供 `{{模板所在文件夹}}`，但路径解析器不接受以变量开头、没有固定根目录的规则。界面能展示或插入的规则不一定能执行。
- **涉及文件**：`src/ui/pages/merge_page.py`、`src/ui/widgets/variable_popup.py`、`src/core/template_merge.py`。
- **推荐修改方式**：实时示例调用正式规划器的单行模式；明确规则语法。若允许 `{{模板所在文件夹}}` 作为受信任系统根目录，应在渲染后重新执行绝对路径和边界校验；否则从“目录变量”列表移除并给出提示。
- **修改收益**：减少界面误导和重复规则代码。
- **风险/影响范围**：可能影响已经输入的试验性规则，应提供清晰迁移提示。

### 5.12 P2：嵌入式输出输入框按行号捕获，删除/移动行后可能写错模板

- **当前问题**：表格内 QLineEdit 的回调捕获创建时的整数行号。模型删除前一行后，视图索引控件与回调生命周期可能不同步，后续编辑可能写入错误项或无效项。
- **涉及文件**：`src/ui/pages/merge_page.py`、`src/ui/models/template_list_model.py`。
- **推荐修改方式**：回调捕获稳定的 `template_id`，通过模型 API 按 ID 更新；结构变化后统一重建 index widgets。长期可改为 delegate，但当前无需为此全面改写表格。
- **修改收益**：避免多模板编辑时规则串行。
- **风险/影响范围**：低；需做“删除第一行后编辑剩余行”的 GUI 回归。

### 5.13 P2：部分失败状态没有在结果界面准确呈现

- **当前问题**：超链接服务能返回 `WARNING` 和逐文件 `error`，但页面报告仍只显示链接数；失败文件看起来像“0 个链接”。多模板扫描存在部分错误时，页面仍用绿色显示“读取完成”，且摘要数量包含失败模板。
- **涉及文件**：`src/ui/pages/replace_page.py`、`src/ui/pages/merge_page.py`、`src/application/merge_service.py`。
- **推荐修改方式**：所有页面统一根据 `TaskState` 呈现成功/警告/失败；详细报告必须列出失败文件和原因；摘要区分“扫描成功数/失败数”。
- **修改收益**：用户能判断结果是否可信，减少静默遗漏。
- **风险/影响范围**：仅展示行为，无业务兼容风险。

### 5.14 P2：关闭窗口在 GUI 线程同步等待，并允许写入中强制退出

- **当前问题**：`closeEvent` 以全局线程池活动数判断任务，缓存刷新等普通任务也会触发退出提示；随后在 GUI 线程 `waitForDone(3000)`，窗口会冻结。超时后允许强制退出，可能正好中断 Word 或磁盘保存。
- **涉及文件**：`src/ui/main_window.py`、`src/application/task_coordinator.py`。
- **推荐修改方式**：由协调器报告真正的用户任务和阶段；退出时发取消请求，使用非阻塞定时检查并显示状态。若处于不可中断的提交/保存阶段，明确禁止强退或先保留可恢复临时文件。
- **修改收益**：退出行为更准确，降低文件损坏风险。
- **风险/影响范围**：会改变退出对话框时序，但不改变业务结果。

### 5.15 P2：大批量规划和预览有不必要的时间与内存开销

- **当前问题**：`plan_merge_jobs` 先保存所有 `raw_jobs`，再生成全部不可变任务，重复持有上下文；预览又为每个 job 线性扫描 Excel 行寻找原数据，最坏为 O(任务数 × 数据行数)。
- **涉及文件**：`src/core/template_merge.py`、`src/application/merge_service.py`。
- **推荐修改方式**：规划时单次生成最终 job，并按需要维护冲突集合；预先构建 `excel_row -> row` 索引。只有确认规模确实很大时再考虑流式执行，不需要引入复杂任务队列。
- **修改收益**：低成本降低大批量内存和预览延迟。
- **风险/影响范围**：重构规划循环时必须保持确定的重名编号顺序。

### 5.16 P2：Windows 路径与原子替换仍缺少真实文件系统验证

- **当前问题**：单个路径段限制为 180 个字符，但没有检查最终完整路径、保留设备名、尾随点/空格组合及 Word 对长路径的实际支持。`os.replace` 用新文件替换目标后，原目标 ACL/额外元数据是否符合预期未验证；网络共享上的语义也不同。
- **涉及文件**：`src/core/template_merge.py`、`src/core/file_utils.py`。
- **推荐修改方式**：增加 Windows 路径预检和可读错误；至少在 NTFS 本地盘、UNC/映射盘各验证一次。保存前记录并按需求恢复必要属性；不要自行实现复杂的 Windows 路径库。
- **修改收益**：减少批量运行到中途才失败和权限变化。
- **风险/影响范围**：路径限制应与 Word 实际能力一致，避免过度拒绝合法长路径。

### 5.17 P2：Word COM 初始化异常被静默吞掉，遍历警告也可能被忽略

- **当前问题**：`CoInitialize()` 的所有异常都被忽略，后续错误会被包装成“无法启动 Word”，根因丢失；StoryRange/Shape 处理中的若干异常也直接 `pass`。此外形状文本既可能属于 story，又被额外枚举，需要验证是否重复计数或替换。
- **涉及文件**：`src/core/word_com.py`、`src/core/replacer_core.py`、`src/core/template_merge.py`。
- **推荐修改方式**：只处理已知可接受的 COM 返回码，其余保留异常链；聚合非致命区域警告并上送 `ServiceResult.WARNING`；用 Windows 样本文档确认形状文本只处理一次。
- **修改收益**：完整模式故障可诊断，统计更可信。
- **风险/影响范围**：可能暴露此前被隐藏的环境问题，这是预期收益。

### 5.18 P2：构建清单和代码质量门槛没有随新模块同步显式维护

- **当前问题**：三个 PyInstaller spec 的 hidden imports 仍主要列旧模块，新模块依赖静态导入通常能被发现，但缺少打包烟雾测试；部分导入已无实际用途。编译检查还产生 `\g` 文档字符串警告。
- **涉及文件**：`WordTextReplacer*.spec`、`src/core/replacer_core.py`、`src/core/file_utils.py`、CI/构建脚本。
- **推荐修改方式**：修正文档字符串为 raw string 或转义反斜杠；清理明确未使用导入；在 Windows 构建后启动打包程序并导入三个页面/服务，执行一个只读样例。hidden imports 仅保留 PyInstaller 无法静态发现的内容，不必机械列出所有包。
- **修改收益**：减少“源码测试通过、发布包启动失败”的风险。
- **风险/影响范围**：低；删除 hidden import 前必须以 clean build 验证。

### 5.19 P3：大文件职责可在稳定后做小范围拆分

- **当前问题**：`merge_page.py` 和 `template_merge.py` 已接近或超过 900 行，输出规则、规划、替换和界面状态集中，后续修改容易产生跨区域影响。
- **涉及文件**：上述两个文件。
- **推荐修改方式**：先完成 P0/P1 并固定测试，再按自然边界拆出 `output_planning.py`、`template_replacement.py` 或页面内部 controller/helper。不要引入新的框架、依赖注入容器或事件总线。
- **修改收益**：降低后续维护认知负担。
- **风险/影响范围**：纯结构调整收益有限，应排在功能稳定之后。

### 5.20 P3：文档需与当前能力和限制同步

- **当前问题**：README 的不可见字符描述与现实现不完全一致，完整模式扫描范围、`.doc` 前提、输出规则语法及 macOS GUI 测试限制未准确说明。
- **涉及文件**：`README.md`、`doc/template_merge_development.md`、用户手册源文件（如有）。
- **推荐修改方式**：只在行为修复并通过 Windows 验证后更新文档；明确快速/完整模式的格式与内容范围差异。
- **修改收益**：减少误用和支持成本。
- **风险/影响范围**：无代码风险。

## 6. 架构与维护性总体评价

### 6.1 建议保持的边界

- 页面负责收集输入和展示结果，不直接实现 Word/Excel 业务。
- 应用服务负责校验、进度、取消和结果状态转换。
- 核心层保持无 Qt 依赖，使用普通数据对象和纯函数规划任务。
- 平台适配层集中管理 Windows/macOS 差异和 Word COM 会话。

### 6.2 当前最应减少的重复

- 输出规则在实时示例、预览、执行中的重复解析；应统一为任务规划器。
- 三个页面各自管理 `_active_worker`、busy 状态、取消和 finished 释放；应由简单的统一任务会话辅助器处理。
- COM StoryRanges 在多个核心函数中自行遍历；应统一复用一个可报告警告的遍历入口。
- UI 预校验和服务执行校验使用不同默认参数；应使用同一输入快照和同一验证函数。

### 6.3 不建议的改动

- 不建议重写为另一套 GUI 框架。
- 不建议为三个页面引入完整 MVVM/Redux、依赖注入容器或消息总线。
- 不建议把所有核心函数改成类，也不建议为很小的工具函数建立多层接口。
- 不建议在没有大规模性能数据前引入数据库、进程池或分布式队列。
- 不建议为了减少文件行数而在 P0/P1 修复前做大规模搬家式重构。

## 7. 推荐实施顺序

每个阶段应独立提交、独立测试；后一个阶段不应成为前一个阶段修复生效的前提。

### 阶段 1：恢复多模板主流程（发布阻断）

1. 为当前多模板页面参数补充失败复现测试。
2. 统一输出规划契约，移除旧全局目录的无条件拦截。
3. 让实时示例、预览和执行共用同一规划器/计划。
4. 验证不同模板根目录、变量子目录、重名编号和旧单模板包装。

**完成标准**：页面预览显示的每个目标路径与实际生成路径一致，正式生成不再返回“请指定输出目录规则”。

### 阶段 2：修复胶囊悬浮窗口和表格编辑生命周期

1. 延后鼠标事件中的弹窗显示，消除 `Qt.Popup` 自动关闭时序冲突。
2. 集中 `show_for` 生命周期并安全管理目标控件引用。
3. 将嵌入式输入框更新从行号绑定改为 `template_id`。
4. 添加原生 Windows GUI 回归测试/人工验收清单。

**完成标准**：同一输入框可反复打开；关闭、插入、切行、删行后均能正确显示并只修改目标模板。

### 阶段 3：统一任务会话和不可变输入快照

1. 选择单任务串行策略，或实现真实的多任务字典；推荐先采用单任务策略。
2. 将扫描、异步预览和写任务全部注册到协调器。
3. 页面启动 worker 前冻结普通 Python 数据快照，锁定所有语义控件。
4. 统一快捷键、底部取消、任务完成和状态栏归属。
5. 将退出流程改为非阻塞等待，并保护保存提交阶段。

**完成标准**：任何入口都不能重复启动冲突任务；取消只影响正确任务；旧 finished 信号不能释放新任务。

### 阶段 4：修复格式校验与扫描/执行一致性

1. 修复两个页面完整模式 `.doc` 的校验参数。
2. 完整模式对 `.doc/.docx/.docm` 使用一致的 COM 扫描范围。
3. 统一 StoryRanges 遍历和警告上报。
4. 修正部分扫描失败、超链接失败的 UI 状态展示。

**完成标准**：Windows 上完整模式可接受 `.doc`；扫描出的变量集合与可替换范围一致；部分失败不会显示成全成功。

### 阶段 5：提高文档内容和保存安全

1. 以测试驱动恢复不可见字符规范化匹配，并保持原始位置映射。
2. 将模糊字段自动匹配改为唯一、可确认的安全策略。
3. 保留多行替换的完整 `rPr`、段落属性和超链接结构。
4. 为 COM 原位保存设计临时副本/失败恢复流程，零命中不备份、不保存。

**完成标准**：复杂格式样本文档前后 XML 对比只出现预期文本变化；保存失败时原文档仍可打开并有恢复路径。

### 阶段 6：Windows 发布验证与低风险整理

1. 在 Windows + 受支持 Microsoft Word 版本运行正文、表格、页眉页脚、文本框、脚注、尾注样例。
2. 验证 NTFS、长路径和至少一种网络共享路径。
3. clean 构建三个发布规格并做打包程序烟雾测试。
4. 修复编译警告、清理明确未使用导入，再同步 README/用户手册。
5. 仅在上述阶段稳定后，按自然职责小范围拆分超大文件。

**完成标准**：源码测试、Windows COM 集成测试、打包烟雾测试均通过，文档描述与实际能力一致。

## 8. 最终结论

项目已经从单文件/旧 GUI 形态向可测试、分层的桌面应用迈出实质一步，更新中的多数基础重构有价值。当前问题主要不是架构需要推倒重来，而是新版多模板输出契约没有贯通、Qt 弹窗与异步任务生命周期不完整，以及 Windows Word 的真实边界尚未形成可靠验证闭环。

建议把 **P0 多模板执行修复、胶囊窗口复现修复、任务协调、`.doc` 完整模式校验** 作为近期最小发布门槛；随后完成 COM 保存安全和格式保真。只要按推荐阶段逐步实施并为每个缺陷补回归测试，无需大规模重写即可显著提高稳定性和可维护性。
