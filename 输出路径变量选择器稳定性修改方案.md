# 输出路径变量选择器稳定性修改方案

> 编制日期：2026-08-24  
> 适用范围：PySide6“模板批量生成”步骤 3  
> 修改目标：用“行内显式按钮 + 共享折叠面板”替代当前悬浮变量窗口，在尽量不改变现有界面的前提下解决崩溃、焦点重入和大量变量展示问题

## 1. 问题说明

当前输出路径输入框在获得焦点或被点击后，会自动打开 `VariableCapsulePopup` 悬浮窗口。现有实现存在以下风险：

1. 悬浮窗口使用 `Qt.Popup`，属于原生顶层弹窗；
2. 输入框同时监听 `FocusIn` 和 `MouseButtonRelease`，可能重复触发打开；
3. 弹窗打开时会改变事件和焦点归属，但代码仍尝试操作弹窗背后的输入框；
4. 每次打开都会异步删除旧变量按钮并立即重新创建全部按钮；
5. 弹窗没有滚动区域和最大高度，Excel 列较多时会超出屏幕；
6. 弹窗保存目标 `QLineEdit` 引用，但缺少模板删除、模型重置和页面销毁时的有效性保护；
7. 当前测试没有覆盖真正显示弹窗、焦点切换、连续打开和大量变量等场景。

实测 500 个数据列时，悬浮窗口高度可达到 3294 px，并被 Qt 放置到屏幕范围之外。即使部分环境不能稳定复现崩溃，该实现也不适合作为跨 Windows、macOS 的正式交互。

## 2. 方案结论

推荐改为：

```text
输出路径输入框右侧的显式“变量”按钮
  → 点击后展开表格下方的共享变量面板
    → 点击变量，将其插入对应输入框的原光标位置
```

目标界面：

```text
Word 模板输出规则
┌──────┬──────────────────┬────────────────────────────────────────┐
│ 启用 │ 模板名称         │ 输出路径与文件名规则                   │
├──────┼──────────────────┼────────────────────────────────────────┤
│  ✓   │ 采购合同         │ D:/输出/{{客户名称}}/...        [变量] │
│  ✓   │ 保密协议         │ D:/输出/{{地区}}/...            [变量] │
└──────┴──────────────────┴────────────────────────────────────────┘

点击“变量”后，在表格下方展开：

┌──────────────────────────────────────────────────────────────────┐
│ 插入到：采购合同                   [搜索变量……]          [关闭] │
│ 系统变量：{{模板名}} {{数据序号}} {{Excel行号}} {{日期}}          │
│ 数据列：  {{客户名称}} {{合同编号}} {{地区}} ……                   │
└──────────────────────────────────────────────────────────────────┘
```

默认状态下变量面板隐藏，不占用页面空间。只有用户明确点击“变量”按钮时才展开。

## 3. 设计原则

- 普通点击输出路径输入框只负责编辑文本，不自动打开变量选择器；
- 变量选择器必须由用户点击显式按钮打开；
- 变量面板是页面内普通 QWidget，不创建顶层弹窗；
- 所有模板行共用一个变量面板，不重复创建大型控件树；
- 大量变量必须在固定高度滚动区域中展示；
- 插入动作使用明确的当前目标模板和光标位置；
- 模板删除、扫描重置或页面关闭后，失效目标不能再被访问；
- 变量数据只在扫描结果变化时刷新，不在每次点击时全部重建；
- Windows 11 Fluent 和 macOS 视觉风格均使用现有主题 token。

## 4. 界面调整范围

### 4.1 输出路径输入框

保留当前步骤 3 的三列表格：

| 启用 | 模板名称 | 输出路径与文件名规则 |
|---|---|---|

不新增“变量”表格列，避免压缩路径输入空间。

在每个 `QLineEdit` 右侧增加一个 trailing action：

```python
action = line_edit.addAction(variable_icon, QLineEdit.TrailingPosition)
```

按钮要求：

- 图标建议使用 `{ }`、`{x}` 或项目现有变量图标；
- tooltip 为“插入变量”；
- accessible name 为“为【模板名称】插入变量”；
- 不使用文字较长的独立按钮，避免占用输入框宽度；
- 深色模式使用主题前景色，不使用固定黑色图标；
- 点击按钮不会改变输入框内容，只有选中具体变量后才插入。

如果 `QLineEdit.addAction()` 的命中区域在高 DPI 下过小，可改为输入框右侧 28–32 px 的 `QToolButton`，但仍应放在同一单元格容器内，不增加表格列。

### 4.2 共享变量面板

在 `tbl_output_templates` 下方、`lbl_live_example` 上方增加一个默认隐藏的面板：

```text
tbl_output_templates
variable_insert_panel  ← 默认隐藏
lbl_live_example
```

建议属性：

- 类型：普通 `QFrame` 或自定义 QWidget；
- 父级：步骤 3 卡片内容区；
- 最大高度：200–220 px；
- 最小高度：120 px；
- 内部变量区使用 `QScrollArea`；
- 不设置 `Qt.Popup`、`Qt.Tool` 或其他顶层窗口 flag；
- 面板展开时由页面现有 `QScrollArea` 处理整体滚动；
- 关闭后恢复隐藏，不保留空白高度。

面板顶部显示：

- 当前目标：“插入到：采购合同”；
- 搜索输入框；
- 关闭按钮。

变量分类：

1. 系统变量；
2. Excel/CSV 数据列；
3. 当前模板的 Word 变量，可选；
4. 最近使用变量，可作为后续增强。

### 4.3 变量胶囊

可继续复用 `VariableCapsuleButton` 的视觉样式，但不再由顶层弹窗管理。

行为：

- 单击插入变量；
- 插入后默认保持面板展开，方便连续插入；
- 插入后恢复目标输入框的光标位置；
- 不重新触发面板打开逻辑；
- 双击不重复插入两次；
- tooltip 显示变量来源和示例值。

建议显示：

```text
{{模板名}}       模板名称
{{数据序号}}     001
{{合同编号}}     HT-001
```

## 5. 交互流程

### 5.1 打开面板

1. 用户在某个模板行点击“插入变量”按钮；
2. 保存目标模板 ID；
3. 保存目标输入框的选择范围或光标位置；
4. 面板标题更新为对应模板名称；
5. 根据扫描结果展示变量；
6. 面板由隐藏变为显示；
7. 搜索框不自动抢占输入框焦点，除非用户主动点击。

如果面板已经打开，点击另一模板行的变量按钮：

- 不关闭再重建面板；
- 只切换目标模板和当前模板变量分组；
- 保持搜索条件或按产品选择清空搜索；
- 推荐清空搜索，避免用户误以为变量缺失。

### 5.2 插入变量

1. 校验目标输入框仍存在且对应模板 ID 仍有效；
2. 恢复保存的光标或选区；
3. 使用 `QLineEdit.insert()` 插入变量；
4. 更新保存后的新光标位置；
5. 触发现有 `textChanged` 和实时示例更新；
6. 可将焦点安全地返回目标输入框，因为面板与输入框位于同一窗口；
7. 面板保持展开。

如果目标已失效：

- 不执行插入；
- 自动关闭面板；
- 显示非阻断提示“目标模板已发生变化，请重新选择”。

### 5.3 关闭面板

以下情况关闭：

- 用户点击面板关闭按钮；
- 用户再次点击当前行的变量按钮，可作为折叠操作；
- 模板被删除；
- 模板模型 reset；
- 重新读取模板与数据；
- 离开模板批量生成页面；
- 主窗口关闭。

以下情况不必关闭：

- 插入一个变量；
- 编辑当前输出规则；
- 修改搜索条件；
- 在变量分类之间切换。

## 6. 搜索与大量变量处理

### 6.1 固定尺寸

变量区必须置于 `QScrollArea` 中，并设置最大高度。禁止根据变量数量调用无限制的 `adjustSize()` 扩张顶层窗口。

建议：

```text
面板最大高度：220 px
变量滚动区最大高度：150–170 px
每行变量胶囊：根据可用宽度自动换行，或固定 3–4 个
```

### 6.2 搜索

变量超过 20 个时，搜索框尤为重要。

搜索范围：

- 变量名称；
- `{{完整占位符}}`；
- 可选的变量说明。

搜索规则：

- 不区分英文大小写；
- 中文包含匹配；
- 输入防抖约 100–150 ms；
- 无结果时显示“未找到匹配变量”；
- 清空搜索恢复全部分类。

### 6.3 控件创建策略

当前实现每次打开都删除并重建全部按钮。建议改为：

- Excel 表头或系统变量列表变化时才刷新变量模型；
- 打开面板时复用已有按钮；
- 搜索时优先隐藏/显示按钮，而不是销毁并重建；
- 模板 Word 变量变化时只刷新对应分组；
- 大于约 300 个变量时可使用 `QListView + QAbstractListModel`，避免创建数百个 QPushButton；
- 首版如果继续使用胶囊按钮，也必须通过滚动区域限制几何尺寸。

## 7. 组件和文件调整

### 7.1 `src/ui/widgets/variable_popup.py`

建议停止使用或移除 `VariableCapsulePopup` 的顶层弹窗实现。

可将文件重构为：

```python
VariableCapsuleButton
VariableInsertPanel
```

`VariableInsertPanel` 负责：

- 变量分组；
- 搜索；
- 滚动展示；
- 变量点击信号；
- 当前目标标题；
- 展开和关闭。

建议由面板发出纯信号：

```python
variableSelected = Signal(str)
closeRequested = Signal()
```

面板不直接持有或操作目标 `QLineEdit`，避免目标生命周期和视图耦合。

### 7.2 `src/ui/pages/merge_page.py`

删除：

- `_PopupEventFilter`；
- 对 `FocusIn` 和 `MouseButtonRelease` 的自动弹出监听；
- `_open_var_popup_for()` 的顶层窗口定位逻辑；
- `self.var_popup`。

新增：

- `self.variable_panel`；
- 当前目标模板 ID；
- 当前目标编辑器的安全引用；
- 保存的光标位置和选择范围；
- `_open_variable_panel(template_id, line_edit)`；
- `_insert_variable_into_target(tag)`；
- `_close_variable_panel()`；
- `_refresh_variable_catalog()`。

在 `_refresh_embedded_path_inputs()` 中：

- 为每个输入框增加 trailing action；
- action 绑定模板 ID 和对应输入框；
- 不安装自动弹出事件过滤器；
- 模型重置前主动关闭变量面板；
- 模型重置后重新建立行内编辑器。

### 7.3 `src/ui/models/template_list_model.py`

原则上不需要改变业务字段。

可补充：

- 通过模板 ID 查找模板的方法；
- 模板删除或 reset 时供页面清除目标状态；
- 输出规则更新仍使用现有 `filename_rule`。

不要让变量面板直接访问模型内部 `_items`。

## 8. 生命周期与安全引用

目标编辑器是通过 `QTableView.setIndexWidget()` 创建的，模型 reset、行删除或页面关闭时可能被销毁。因此不能只保存一个永久 Python 引用。

推荐同时保存：

- `template_id`，作为业务身份；
- 持有弱引用或 Qt 安全指针语义的编辑器引用；
- 光标和选区数据。

插入前必须验证：

```text
页面仍有效
→ 模板 ID 仍存在
→ 编辑器仍有效
→ 编辑器仍属于该模板行
→ 才允许插入
```

页面关闭、模型 reset、行删除和重新扫描时统一调用 `_close_variable_panel()`，清空全部目标状态。

## 9. 主题与无障碍

### 9.1 主题

- 面板背景使用 `card_bg`；
- 边框使用 `border_subtle` 或 `border_strong`；
- 搜索框使用 `entry_bg/entry_border`；
- 胶囊使用 `button_bg/button_hover/button_pressed`；
- 选中或当前目标使用 `accent`；
- 不使用硬编码白色、黑色或浅灰背景；
- 深色模式切换后立即刷新面板和按钮样式。

### 9.2 无障碍

- 行内按钮 accessible name 包含模板名称；
- 面板 accessible name 为“输出规则变量选择器”；
- 分类标题可被读屏识别；
- 键盘可通过 Tab 进入搜索框和变量项；
- Enter/Space 插入变量；
- Esc 关闭面板并返回目标输入框；
- 错误状态不能只依赖颜色。

## 10. 测试方案

### 10.1 单元测试

- 变量分类数据正确；
- 搜索过滤正确；
- 变量点击发出一次 `variableSelected`；
- 面板打开和关闭状态正确；
- 变量超过 500 个时面板高度不超过上限；
- 搜索无结果提示正确；
- 主题刷新不丢失变量数据。

### 10.2 MergePage GUI 测试

- 普通点击输出路径不会自动显示变量面板；
- 点击行内变量按钮才显示面板；
- 面板显示的目标模板名称正确；
- 点击变量插入到原光标位置；
- 有选区时变量替换选中内容；
- 连续插入多个变量只各插入一次；
- 切换模板行后插入到新目标；
- 编辑一个模板不会影响其他模板；
- 模板删除时面板自动关闭；
- 模型 reset 时不会访问已销毁输入框；
- 页面关闭时没有延迟回调访问旧控件；
- 反复打开/关闭 100 次无崩溃、无明显控件泄漏。

### 10.3 大量变量测试

- 20、100、500、1000 个 Excel 列；
- 面板高度始终在限制内；
- 页面和面板不会出现在屏幕范围之外；
- 搜索和滚动保持响应；
- 不在每次打开时重复创建全部控件；
- 内存不会随打开次数持续增长。

### 10.4 平台测试

- Windows 11：鼠标、触控板、高 DPI、深浅色；
- macOS：Cocoa 原生窗口、触控板、Retina、深浅色；
- offscreen 自动化测试；
- PyInstaller Windows EXE 和 macOS App 实际打包版本。

## 11. 实施顺序

### P0：消除崩溃路径

1. 移除 `Qt.Popup` 悬浮窗口；
2. 移除 FocusIn/MouseButtonRelease 自动打开；
3. 增加行内显式变量按钮；
4. 新增普通 QWidget 折叠面板；
5. 增加固定最大高度和滚动区域；
6. 增加目标编辑器有效性保护；
7. 补充显示、切换和生命周期测试。

### P1：完善使用体验

1. 增加搜索；
2. 增加系统变量、数据列、模板变量分类；
3. 保存并恢复光标和选区；
4. 增加键盘和读屏支持；
5. 完成深浅色视觉检查。

### P2：性能增强

1. 缓存变量控件；
2. 大量变量改用 Model/View；
3. 最近使用变量；
4. 常用变量置顶；
5. 支持用户自定义变量收藏。

## 12. 验收标准

1. 普通点击输出路径不再自动弹出任何顶层窗口；
2. 每行输出路径右侧都有明确的变量按钮；
3. 点击按钮后只在步骤 3 内展开一个共享面板；
4. 面板关闭时不占用页面高度；
5. 变量可插入到正确模板、正确光标位置；
6. 连续插入和切换模板不会重复打开或插错输入框；
7. 500 个以上变量时面板仍在屏幕范围内并可滚动搜索；
8. 模板删除、模型 reset、重新扫描和页面关闭不会访问失效控件；
9. 反复打开、关闭和切换目标无崩溃、无持续内存增长；
10. Windows 和 macOS 打包版本均通过实机验证；
11. 深色模式无白色边框、文字重影或编辑器残影；
12. 当前输出规则编辑、实时示例和批量生成行为保持不变。

## 13. 最终建议

不建议继续修补现有顶层 `Qt.Popup` 的位置和焦点逻辑。即使增加防重入，它仍然需要处理原生窗口、外部输入框焦点、异步控件销毁和超大尺寸等问题。

“行内显式按钮 + 页面内共享折叠面板”只在当前输入框增加一个小图标，平时不改变布局；打开时在表格下方增加一块固定高度的滚动区域。该方案改动范围可控，也能同时解决稳定性、可发现性、大量变量和跨平台一致性问题。
